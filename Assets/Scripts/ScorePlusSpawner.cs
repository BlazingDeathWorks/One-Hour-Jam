using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ScorePlusSpawner : MonoBehaviour
{
    [SerializeField] ScorePlus[] scorePlus;

    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
